<?xml version="1.0" encoding="UTF-8"?>
<configuration scan="true" period="10 seconds">

    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <target>System.out</target>
        <encoder>
            <pattern>
                [%-5level] %date{[yyyy-MM-dd HH:mm:ss.SSS]} [%X{akkaSource}] [%logger{0}] %msg in [%thread]%n
            </pattern>
        </encoder>
    </appender>

    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>C:/Projects/CSV2Mongo/log/csv.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>C:/Projects/CSV2Mongo/log/-%d{yyyy-MM-dd}.%i.zip</fileNamePattern>
            <timeBasedFileNamingAndTriggeringPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedFNATP">
                <maxFileSize>100MB</maxFileSize>
            </timeBasedFileNamingAndTriggeringPolicy>
            <maxHistory>7</maxHistory>
            <totalSizeCap>5GB</totalSizeCap>
        </rollingPolicy>
        <encoder>
            <pattern>
                [%-5level] %date{[yyyy-MM-dd HH:mm:ss.SSS]} [%X{akkaSource}] [%logger{0}] %msg in [%thread]%n
                <!--%date{yyyy-MM-dd HH:mm:ss} %-5level %logger{0} %msg in [%thread]%n-->
            </pattern>
        </encoder>

    </appender>

    <appender name="JobLogFile" class="ch.qos.logback.classic.sift.SiftingAppender">
        <discriminator>
            <key>strategy</key>
            <defaultValue>strategy</defaultValue>
        </discriminator>
        <sift>
            <appender name="FILE-${strategy}" class="ch.qos.logback.core.rolling.RollingFileAppender">
                <file>C:/Projects/CSV2Mongo/log/${strategy}.log</file>
                <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
                    <fileNamePattern>
                        C:/Projects/CSV2Mongo/log/${strategy}-%d{yyyy-MM-dd}.%i.zip
                    </fileNamePattern>
                    <maxFileSize>20MB</maxFileSize>
                    <maxHistory>10</maxHistory>
                    <totalSizeCap>5GB</totalSizeCap>
                </rollingPolicy>
                <encoder>
                    <pattern>
                        [%-5level] %date{[yyyy-MM-dd HH:mm:ss.SSS]} [%X{akkaSource}] [%logger{0}] %msg in [%thread]%n
                    </pattern>
                </encoder>

            </appender>
        </sift>
    </appender>


    <logger name="akka" level="DEBUG"/>
    <logger name="akka._" level="DEBUG"/>
    <logger name="reactivemongo" level="INFO"/>
    <logger name="reactivemongo._" level="INFO"/>
    <logger name="KafkaConsumer" level="INFO"/>

    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="FILE"/>
        <appender-ref ref="JobLogFile"/>
    </root>

</configuration>